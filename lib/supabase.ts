/**
 * Browser-only Supabase client (safe to import from Client Components).
 * For server usage, import from `@/lib/supabase/server`.
 */
export { createSupabaseBrowserClient } from "./supabase/browser"
